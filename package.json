{
  "name": "freezedrymachine",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test:frontend": "node --test src/features/source/folderDrop.test.ts src/chartTimeline.test.ts src/csvByteHandling.test.ts src/csvFileOrdering.test.ts src/incrementalSamples.test.ts src/useBrowserCsvTail.test.ts",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "collector:dev": "cargo run -p collector",
    "collector:check": "cargo check -p collector",
    "local:serve": "npm run build && cargo run -p collector",
    "check": "npm run test:frontend && npm run build && cargo check -p collector"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.101.1",
    "@tauri-apps/api": "^2.11.1",
    "echarts": "^6.1.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.3",
    "@types/node": "^26.0.0",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
